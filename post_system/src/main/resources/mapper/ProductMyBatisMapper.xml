<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.snorlax.repository.mybatis.ProductMyBatisMapper">

    <select id="searchByKeyword" resultType="Product">
        SELECT *
        FROM product
        WHERE store_id = #{storeId}
          AND (
              LOWER(name) LIKE CONCAT('%', LOWER(#{query}), '%')
              OR LOWER(brand) LIKE CONCAT('%', LOWER(#{query}), '%')
              OR LOWER(sku) LIKE CONCAT('%', LOWER(#{query}), '%')
          )
    </select>

</mapper>
